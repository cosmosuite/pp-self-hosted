# ─── Compute server (Hetzner VPS) ────────────────────────────────────────────
COMPUTE_URL=http://YOUR_HETZNER_IP:8000
COMPUTE_API_KEY=change-me-to-a-random-secret

# ─── Server ──────────────────────────────────────────────────────────────────
PORT=4000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ─── Database (Railway PostgreSQL) ───────────────────────────────────────────
DATABASE_URL=postgresql://user:pass@host:5432/safevision

# ─── Railway Bucket Storage (S3-compatible) ──────────────────────────────────
# These are auto-injected by Railway when you link a bucket to your service.
# For local dev, set them manually if you want storage.
# BUCKET=my-bucket-abc123
# ACCESS_KEY_ID=
# SECRET_ACCESS_KEY=
# REGION=auto
# ENDPOINT=https://storage.railway.app

# ─── useautumn.com credits ───────────────────────────────────────────────────
AUTUMN_SECRET_KEY=
AUTUMN_ENABLED=false
